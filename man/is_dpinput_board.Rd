% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_get.R
\name{is_dpinput_board}
\alias{is_dpinput_board}
\title{Check if Board References Data Product Input (internal)}
\usage{
is_dpinput_board(board_object)
}
\arguments{
\item{board_object}{A \code{pins_board} object from \code{dp_connect}. Can be a
local board, LabKey board, or S3 board.}
}
\value{
Logical value: \code{TRUE} if the board references a data product input
folder, \code{FALSE} if it references a regular data product folder.
}
\description{
Internal helper function that determines whether a pins board
object is configured to reference a data product input folder (dpinput)
based on the board type and its path configuration.
}
\details{
The function examines the board's path configuration:
\itemize{
\item For \code{pins_board_folder}: checks the \code{path} attribute
\item For \code{pins_board_labkey}: checks the \code{subdir} attribute
\item For \code{pins_board_s3} boards: checks the \code{prefix} attribute
}

It splits the path by underscores, hyphens, and slashes, then checks if
the last component equals "dpinput".
}
\keyword{internal}
